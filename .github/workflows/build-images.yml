name: Build and Push Docker Images

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: build-images-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-backend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@nightly

      - name: Install Cranelift codegen backend
        run: rustup component add rustc-codegen-cranelift-preview --toolchain nightly

      - name: Install native build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang lld libssl-dev pkg-config

      - name: Cache Cargo build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: backend -> target

      - name: Inspect build resources
        run: |
          free -h
          nproc
          if [ -f /sys/fs/cgroup/memory.max ]; then cat /sys/fs/cgroup/memory.max; fi
          if [ -f /sys/fs/cgroup/memory.swap.max ]; then cat /sys/fs/cgroup/memory.swap.max; fi

      - name: Serialize Rust compiler processes
        run: |
          cat > "$RUNNER_TEMP/serial-rustc" <<'EOF'
          #!/usr/bin/env bash
          set -euo pipefail
          exec flock "${RUNNER_TEMP:-/tmp}/tokensbyte-rustc.lock" "$@"
          EOF
          chmod +x "$RUNNER_TEMP/serial-rustc"
          echo "RUSTC_WRAPPER=$RUNNER_TEMP/serial-rustc" >> "$GITHUB_ENV"

      - name: Build backend binary on runner
        env:
          CARGO_BUILD_JOBS: "1"
          CARGO_BUILD_PIPELINING: "false"
          CARGO_INCREMENTAL: "0"
          CARGO_PROFILE_DEV_CODEGEN_BACKEND: "cranelift"
          CARGO_PROFILE_DEV_OPT_LEVEL: "0"
          CARGO_PROFILE_DEV_DEBUG: "0"
          CARGO_PROFILE_DEV_INCREMENTAL: "false"
          CARGO_PROFILE_DEV_STRIP: "symbols"
          CARGO_PROFILE_DEV_PANIC: "abort"
          RUSTFLAGS: "-Awarnings -Zthreads=1 -Zmir-opt-level=0 -Zinline-mir=false -Zshare-generics=y -C opt-level=0 -C debuginfo=0 -C panic=abort"
        run: |
          cargo +nightly build -Zcodegen-backend --manifest-path backend/Cargo.toml --profile dev --locked -j "$CARGO_BUILD_JOBS"
          cp backend/target/debug/tokensbyte-server backend/tokensbyte-server
          chmod +x backend/tokensbyte-server

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Resolve image name
        id: image
        env:
          OWNER: ${{ github.repository_owner }}
        run: echo "name=ghcr.io/${OWNER,,}/tokensbyte-backend" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile.thin
          platforms: linux/amd64
          push: true
          tags: |
            ${{ steps.image.outputs.name }}:latest
            ${{ steps.image.outputs.name }}:${{ github.sha }}
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
          cache-from: type=gha,scope=tokensbyte-backend
          cache-to: type=gha,mode=max,scope=tokensbyte-backend

  build-frontend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Build frontend assets
        working-directory: frontend
        run: |
          npm install --ignore-scripts --no-audit --no-fund --package-lock=false
          npm run build

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Resolve image name
        id: image
        env:
          OWNER: ${{ github.repository_owner }}
        run: echo "name=ghcr.io/${OWNER,,}/tokensbyte-frontend" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push frontend
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ steps.image.outputs.name }}:latest
            ${{ steps.image.outputs.name }}:${{ github.sha }}
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
          cache-from: type=gha,scope=tokensbyte-frontend
          cache-to: type=gha,mode=max,scope=tokensbyte-frontend
